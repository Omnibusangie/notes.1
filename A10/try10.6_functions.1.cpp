// functions testing: encrypt

#include <iostream>
#include <cstdlib>
#include <ctime>
#include <string>

using namespace std;

string encrypt(string text, int shift);

	int main()
	{
		char enter;
		string text;
		int shift;
	
		cout << "Enter text in capital letters: ";
		cin >> text;
	
		cout << "Enter shift code: ";
		cin >> shift;
		srand(shift);
	}

string encrypt(string text, int shift) // the stuff inside the parentheses are the variables used in this function
{	
	string encrypted;
	
	
	
	return encrypted;	
}
