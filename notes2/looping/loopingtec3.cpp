//technique 3 for looping

#include <iostream>
#include <cstdlib>
#include <ctime>

using namespace std;

int main()

{
	/* int, short, long long, float, double, char, bool
	
	int num_times = 0;
	while(num_times(10))
	{
	num_times = num_times +1
	*/
	
	// this is the new technique in case you have a huge code block
	
	for(int num_times = 0; num_times < 10; num_times ++)
	{
	}
	
	//there can be nesting loops within the code block using this third technique
	
	return 0;
}
